# =============================================================
# RAG-DPO — Dépendances directes (versions pinnées)
# Généré depuis l'environnement local fonctionnel (2026-03-05)
# =============================================================

# --- Core / Scraping ---
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2
scrapy==2.14.1
playwright==1.57.0

# --- Document processing ---
PyPDF2==3.0.1
python-docx==1.2.0
openpyxl==3.1.5
odfpy==1.4.1
pdfplumber==0.11.9
PyMuPDF==1.26.7

# --- OCR (optionnel, pour PDFs scannés) ---
pytesseract==0.3.13

# --- Images ---
Pillow==12.1.0

# --- Data ---
pandas==2.3.3
numpy==2.4.1

# --- RAG & LLM ---
chromadb==1.4.1
sentence-transformers==5.2.0
transformers==4.57.6
einops==0.8.2
ollama==0.6.1
rank-bm25==0.2.2
FlagEmbedding==1.3.5

# --- Agent ---
langgraph==1.0.7
langchain-core==1.2.7

# --- PyTorch ---
# GPU (CUDA 12.6) : pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# CPU only (Docker) : pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch==2.10.0

# --- Utils ---
python-dotenv==1.2.1
PyYAML==6.0.3
tqdm==4.67.1
colorama==0.4.6
pydantic==2.12.5

# --- Interface ---
streamlit==1.53.1

# --- Dev ---
black==26.1.0
flake8==7.3.0
pytest==9.0.2
